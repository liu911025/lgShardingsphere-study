spring:
  profiles:
    active: sharding-databases-tables

mybatis:
  # config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mappers/**/*.xml
  type-aliases-package: com.study.sharding.pojo